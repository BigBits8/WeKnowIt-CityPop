import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container:{
    marginBottom: 100,
  },
  text: {
    fontSize: 40,
    fontWeight: "500",
  },
});


export default styles;
